-- MySQL / PostgreSQL
SELECT CURRENT_DATE;
SELECT CURRENT_TIME;
SELECT NOW(); -- current date & time

-- SQL Server
SELECT GETDATE();       -- current date & time
SELECT CONVERT(DATE, GETDATE());  -- just the date
